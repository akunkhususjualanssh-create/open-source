# open-source


# update dulu : 
``` bash
apt update && apt upgrade -y
```

# install script langsung 
```bash 
apt install -y && apt update -y && apt upgrade -y && apt install lolcat -y && gem install lolcat && wget -q https://raw.githubusercontent.com/akunkhususjualanssh-create/open-source/main/premi.sh && chmod +x premi.sh && ./premi.sh
